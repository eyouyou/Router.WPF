<UserControl x:Class="Router.WPF.Sample.Views.ModerationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:r="clr-namespace:Router.Wpf;assembly=Router.WPF"
             Background="{StaticResource BgBrush}">
    <Grid Margin="40,32" MaxWidth="960" HorizontalAlignment="Stretch">
        <DockPanel>
            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,20">
                <TextBlock Text="Moderation" Style="{StaticResource H1}"/>
                <TextBlock Text="Two levels of nested outlets, plus an IndexRoute as the default child of /blocked-users." Style="{StaticResource Muted}" Margin="0,0,0,0"/>
            </StackPanel>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource Card}" Padding="8" Margin="0,0,16,0">
                    <StackPanel>
                        <TextBlock Text="SECTIONS" Style="{StaticResource Label}" Margin="10,8,0,8"/>
                        <Button Style="{StaticResource SidebarButton}" Foreground="{StaticResource TextBrush}"
                                Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="/moderation/blocked-users">
                            <TextBlock Text="Blocked users"/>
                        </Button>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource Card}" Padding="0">
                    <r:Outlet/>
                </Border>
            </Grid>
        </DockPanel>
    </Grid>
</UserControl>
